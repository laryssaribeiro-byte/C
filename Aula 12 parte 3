/*comparações pode sem tão complexas quanto queiramos
como verificar se um triangulo é isóceles
Criador:Laryssa Ribeiro
Data 03/02/2026*/

#include <stdio.h>

int main(void)
{
    int lado1, lado2, lado3;

    scanf("%d\n %d\n %d", &lado1, &lado2, &lado3);

        if (((lado1 == lado2) && (lado1 != lado3)) ||
            ((lado2 == lado3) && (lado2 != lado1)) ||
            ((lado1 == lado3) && (lado1 != lado2)))
        {
            // dois lado são iguais e um é diferente
            printf("Triangulo e isoceles");
        }

    return 0;
}
